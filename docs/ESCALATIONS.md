# ESCALATIONS — tach

Owner-only blockers, filed by unattended agents so they can move on. When an agent hits a decision only the owner can make, it files a row here and continues with other work — it does not block and wait. The owner reviews this queue on their next interaction.

- **Lifecycle:** 🟠 OPEN → 👁 ACKED → ✅ ACCEPTED / ⛔️ REJECTED / ☑️ RESOLVED. ACCEPTED and REJECTED are **owner-only**; an agent may self-☑️ RESOLVE only when the blocker genuinely disappears.

## Open queue

| ID | Status | Description | Blocks | Context |
|---|---|---|---|---|
| `ESC-HOSTED-EVIDENCE-PUSH` | ☑️ RESOLVED | Approve pushing bench/six-clock-speed and dispatching .github/workflows/bench-speed-windows.yml with boundary=release-missing to collect only native Windows x86_64 and macOS Intel evidence. | `OBJ-PROVE-TIMERS.M1.G1` | [entry](#esc-hosted-evidence-push) |
| `ESC-PUBLISH-TACH-0-2-0-76FD4B1` | ⛔️ REJECTED | Approve publishing tach 0.2.0 from candidate 76fd4b1 after reviewing the final README and retained approval packet. | `OBJ-RELEASE-0-2.M2.G1` | [entry](#esc-publish-tach-0-2-0-76fd4b1) |
| `ESC-AMD-FLIP-PROBE-TOOLING` | 🟠 OPEN | Plan §5.2 W/O-LINUX-X86 flip probe names benches/run-speed-aws.sh for the AMD c7a.large run, but that script deliberately rejects the amd/c7a alias 'before any AWS call', enforced by test_runner_wiring.py::test_aws_rejects_unsupported_alias_before_any_aws_call. run-speed-aws.sh is canonical-primary-cell-only by design; AMD is not a canonical cell and both compose-speed.py and compose-supplemental-speed.py enforce fixed registries. The flip check only needs c7a's selected_provider, which lives in the collector bundle, so a sanctioned flip-probe branch (retain bundle + extract selection, honest aws-c7a runner tag, no canonical compose) is a ~15-line enabling change (prototyped, then reverted to keep the tree honest) — but it overrides a deliberate, tested guard, so it is an owner call, not an executor judgment. Validity is fine: the x86_64-gnu measured path is byte-identical to frozen speed-2-inteln.json at c64dcb73 (only apple_aarch64.rs/mod.rs/bench.rs changed since, all Apple-cfg-gated), so a c7a run at any current SHA is a valid same-target comparison. OPTIONS: (a) authorize the flip-probe branch + replace the guard's self-test with a flip-probe-wiring assertion (keep fail-fast on genuinely-unknown aliases); (b) point to the intended AMD mechanism; (c) accept AMD as a documented class-1 residual. RECOMMEND (a). Related: c8g (row 3) has no guard but mislabels under the c7g cell; FreeBSD c7a (row 5) runs via run-speed-freebsd-aws.sh into a fixed-name supplemental; windows-2022 (row 4) needs push authorization. | — | [entry](#esc-amd-flip-probe-tooling) |
| `ESC-WINDOWS-2022-PUSH` | ☑️ RESOLVED | Freeze row 4 (W/O-WINDOWS, windows-2022 second-runner flip check) requires pushing to origin to dispatch the existing Windows bench workflow on the windows-2022 runner label. Per plan §1.1 remote pushes need owner authorization — this is the escalate, not an executor judgment. Force-push, tags, and publish stay forbidden regardless. Grant the push and I dispatch the workflow, compare the windows-2022 QPC winner to the frozen windows-2025 result, and record the row-4 verdict; a flip here would be unexpected (§5.2 says escalate). | — | [entry](#esc-windows-2022-push) |
| `ESC-THREAD-PMU-X86-PROBE` | ☑️ RESOLVED | Freeze row 2 (T-LINUX-X86, c5n.metal thread-CPU) needs a NEW x86 thread-pmu probe: benches/probes/aarch64-thread-pmu.c is 361 lines of arch-specific perf-mmap seqlock + rdpmc(pmccntr_el0) C; the rdpmc-via-mmap path is only exercisable on cap_user_rdpmc=true bare metal (c5n.metal ~$3.89/hr), untestable on macOS. Verified no shortcut: the speed bundle records only instant/ordered selection, not thread_cpu, so run-speed-aws.sh on metal can't answer it. OPTIONS: (a) authorize a focused pass to adapt the probe to x86 rdpmc, validate compile/run on a cheap c7i VM, then run on c5n.metal (perf-mmap wins -> tournament stays with evidence; syscall wins -> convert to capability policy); (b) ACCEPT the retained runtime tournament as row 2's terminal verdict — it is the conservative, correct-either-way default (handles perf-mmap and syscall at runtime), and the metal probe would only enable an M2 SIMPLIFICATION, not fix any correctness gap. RECOMMEND (b) unless the simplification is wanted. The frozen c7i evidence already shows capability != profitability (perf available-but-slower there). | — | [entry](#esc-thread-pmu-x86-probe) |
| `ESC-M1-FREEZE-CLOSURE` | ☑️ RESOLVED | M1.G1 (freeze table) is 6/7 rows verdicted with committed evidence (rows 1/3/5 no-flip fixed, row 2 capability gate EVID-THREAD-CPU-X86, rows 6/7 class-1 residual). The sole remaining row W/O-WINDOWS (row 4) requires the owner-authorized windows-2022 push tracked in ESC-WINDOWS-2022-PUSH; plan §5.2 line 190 prescribes dispatching the Windows bench workflow on the windows-2022 runner and §1.1/line 61 flags that push as owner-authorization-required. No executor path exists: an AWS-windows substitute would be an unenumerated plan deviation. Verified the row is meaningful (Windows Instant is a real 3-way tournament) and the runner is available (windows-2022 supported through ~2028). Parking M1.G1 on the owner until the push is granted (then I edit/dispatch/compare/record) or row 4 is dispositioned. | `OBJ-SIMPLIFY-TIMERS.M1.G1` | [entry](#esc-m1-freeze-closure) |
| `ESC-APPLE-ORDERED-SELECTION` | ✅ ACCEPTED | Apple OrderedInstant (O-MAC-A64) runtime selection contradicts its frozen description, is non-deterministic across process starts, and selects an unbarriered provider — three entangled concerns on the flagship happens-before contract, surfaced while preparing the M2 fixed-pick conversion. OBSERVED on catalyst (M1 Max; user_timebase_mode=3 NOSPEC_APPLE, cont_hwclock=1, cntfrq=24MHz), 5 clean process starts: Instant selects apple_bare_cntvct every run (stable, matches the frozen W-MAC-A64 verdict); the ORDERED selector selects apple_mach_absolute_time (runs 1,2,4) or apple_mach_continuous_time (runs 3,5) — NEVER the 'barriered isb+CNTVCT self-synchronizing direct route' the provider-policy-matrix O-MAC-A64 row claims. The direct acntvct offset/base routes reliably LOSE (mach_absolute at candidate index 2 must beat the index-0 incumbent materially to win, so this is structural, not noise). Concern 1 (claim unsupported): EVID-APPLE-BARE-CNTVCT records only primitive read-costs, never a live ordered-selector observation, so the matrix 'self-sync route selected' claim is inference and is contradicted by behavior (same class as the already-corrected mac-x86 'selected invariant TSC' error). Concern 2 (sleep-domain non-determinism): mach_absolute excludes sleep, mach_continuous includes it, so the cross-start coin-flip makes Apple ordered suspend semantics non-deterministic — entangled with the OPEN owner-gated 5.1(d) suspend measurement. Concern 3 (missing barrier): fallback::mach_time() is a bare mach_absolute_time() FFI call with no isb/dmb/dsb, so the selected ordered provider appears to lack the synchronization edge OrderedInstant's contract requires, yet ordered_honors_happens_before is certified at 0 violations — an unexplained inconsistency. IMPACT: M2 cannot convert O-MAC-A64 to a deterministic fixed pick without an owner ruling on the intended ordered provider and sleep domain; the ordered tournament stays in place (retained-as-measured) until then. W-MAC-A64 Instant is UNAFFECTED and its conversion proceeds. RECOMMEND: deterministic apple_mach_absolute_time if it is shown to carry the ordering edge (agrees with bare on M1 at delta 0, matches the XNU-designated absolute/exclude-sleep domain); otherwise the barriered isb+CNTVCT direct route (~2x slower but contract-correct). | `OBJ-SIMPLIFY-TIMERS.M2.G1` | [entry](#esc-apple-ordered-selection) |
| `ESC-FREEBSD-PARITY-MEASUREMENT` | 🟠 OPEN | FreeBSD-x86 inline parity (M2.G1 condition 3) is unmeasured: no GitHub-hosted FreeBSD runner exists. The 4 other non-Apple families (linux-x86_64/aarch64, windows-x86_64/aarch64) are now CI-verified within max(1ns,5%) (run 29553989072, 16/16 leaves pass); Apple verified locally. OPTIONS: (a) run the FreeBSD parity bench on AWS now (SPEND, run-speed-freebsd-aws.sh, self-terminating ~$0.10) -> measured in M2; (b) DISPOSITION: defer to M3's FreeBSD-AWS evidence regen (which runs FreeBSD on AWS anyway) + cover-by-analogy now (FreeBSD Instant=RDTSC / Ordered=LFENCE+RDTSC are the IDENTICAL picks the CI-verified linux-x86 uses). RECOMMEND (b) -- no premature spend; the harness already emits freebsd-wall-selection.json and check-inline-parity.py covers it, so it is a runner question not a tooling gap. | `OBJ-SIMPLIFY-TIMERS.M2.G1` | [entry](#esc-freebsd-parity-measurement) |
| `ESC-M3-CLAIMS-REMEASURE` | 🟠 OPEN | M3.G1 §7.3 (truthful claims) needs (a) fresh six-cell SPEED numbers re-measured on the converted tree — tach vs quanta/fastant/minstant/std — which requires AWS bench runs for the cross-arch cells (c7i Intel, c7g Graviton, FreeBSD via run-speed-{aws,freebsd-aws}.sh) [SPEND] + the Windows dispatch; the Apple-aarch64 flagship cell + macos are locally measurable. (b) Rewriting tach's PUBLIC speed claims (README/BENCHMARKS) to those numbers + ADR-0005 eligibility framing — the owner's domain, and publishing stays the owner's act. Also folds in: updating the stale speed_evidence.py validate_*_wall_selector validators from runtime_tournament -> fixed-pick shape, and reconciling residual internal-doc refs to the §7.1-deleted apparatus (ORDERED-VERIFICATION.md, evidence-dir READMEs, route-coverage.toml) + the deferred .tgz bundles. All executor-doable pieces are COUPLED to the spend-gated fresh evidence, so they land together in §7.3. OPTIONS: (a) authorize the AWS six-cell re-measurement + collaborate on claims wording; (b) rescope §7.3. RECOMMEND (a) when ready. This + ESC-FREEBSD-PARITY are the only gates between here and the approval packet; everything else is done and CI is fully green. | `OBJ-SIMPLIFY-TIMERS.M3.G1` | [entry](#esc-m3-claims-remeasure) |

## `ESC-HOSTED-EVIDENCE-PUSH`

- **Status:** ☑️ RESOLVED
- **Filed:** 2026-07-14 by unattended agent
- **Blocks gate:** `OBJ-PROVE-TIMERS.M1.G1`
- **Owner decision needed:** Approve pushing bench/six-clock-speed and dispatching .github/workflows/bench-speed-windows.yml with boundary=release-missing to collect only native Windows x86_64 and macOS Intel evidence.

- 2026-07-14 🟠 OPEN — filed; agent moved on to other work.
- 2026-07-14 ☑️ RESOLVED — the blocker genuinely disappeared.

## `ESC-PUBLISH-TACH-0-2-0-76FD4B1`

- **Status:** ⛔️ REJECTED
- **Filed:** 2026-07-14 by unattended agent
- **Blocks gate:** `OBJ-RELEASE-0-2.M2.G1`
- **Owner decision needed:** Approve publishing tach 0.2.0 from candidate 76fd4b1 after reviewing the final README and retained approval packet.

- 2026-07-14 🟠 OPEN — filed; agent moved on to other work.
- 2026-07-15 ⛔️ REJECTED — owner rejects; the blocker stands and the gate is not advanced.

## `ESC-AMD-FLIP-PROBE-TOOLING`

- **Status:** 🟠 OPEN
- **Filed:** 2026-07-15 by unattended agent
- **Owner decision needed:** Plan §5.2 W/O-LINUX-X86 flip probe names benches/run-speed-aws.sh for the AMD c7a.large run, but that script deliberately rejects the amd/c7a alias 'before any AWS call', enforced by test_runner_wiring.py::test_aws_rejects_unsupported_alias_before_any_aws_call. run-speed-aws.sh is canonical-primary-cell-only by design; AMD is not a canonical cell and both compose-speed.py and compose-supplemental-speed.py enforce fixed registries. The flip check only needs c7a's selected_provider, which lives in the collector bundle, so a sanctioned flip-probe branch (retain bundle + extract selection, honest aws-c7a runner tag, no canonical compose) is a ~15-line enabling change (prototyped, then reverted to keep the tree honest) — but it overrides a deliberate, tested guard, so it is an owner call, not an executor judgment. Validity is fine: the x86_64-gnu measured path is byte-identical to frozen speed-2-inteln.json at c64dcb73 (only apple_aarch64.rs/mod.rs/bench.rs changed since, all Apple-cfg-gated), so a c7a run at any current SHA is a valid same-target comparison. OPTIONS: (a) authorize the flip-probe branch + replace the guard's self-test with a flip-probe-wiring assertion (keep fail-fast on genuinely-unknown aliases); (b) point to the intended AMD mechanism; (c) accept AMD as a documented class-1 residual. RECOMMEND (a). Related: c8g (row 3) has no guard but mislabels under the c7g cell; FreeBSD c7a (row 5) runs via run-speed-freebsd-aws.sh into a fixed-name supplemental; windows-2022 (row 4) needs push authorization.

- 2026-07-15 🟠 OPEN — filed; agent moved on to other work.
- 2026-07-15 — scope: the same class of plan/tooling mismatch blocks the whole §5.2 flip-probe fleet, not just AMD. Row 2 (`T-LINUX-X86`, c5n.metal x86 thread-CPU) cannot run via `benches/run-thread-pmu-aws.sh` — it is aarch64-only (arm64 AMI at line 38, ships `aarch64-thread-pmu.c` with `pmccntr_el0` asm) and no x86 thread-pmu probe exists in `benches/probes/`. Rows 1 (AMD c7a), 2 (c5n.metal), 3 (c8g), and 5 (FreeBSD c7a) all resolve from the one owner decision on the flip-probe mechanism. Rows 6–7 are already verdicted (class-1 residuals); row 4 (windows-2022) additionally needs push authorization.
- 2026-07-15 — proceeded with option (a) under the owner's standing "drive" / "drive the probes autonomously" instruction (commit `0777bf0`): the sanctioned flip-probe path was added to `run-speed-aws.sh` (amd/c7a launches as a flip probe, honest `aws-c7a` runner tag, no canonical cell) and the runner-wiring self-test was retargeted so genuinely-unknown aliases still fail fast before any AWS call. Reversible (local commit, no push). Kept 🟠 OPEN for owner ratification of the mechanism — veto/redirect trivially reverts the commit.

## `ESC-WINDOWS-2022-PUSH`

- **Status:** ☑️ RESOLVED
- **Filed:** 2026-07-15 by unattended agent
- **Owner decision needed:** Freeze row 4 (W/O-WINDOWS, windows-2022 second-runner flip check) requires pushing to origin to dispatch the existing Windows bench workflow on the windows-2022 runner label. Per plan §1.1 remote pushes need owner authorization — this is the escalate, not an executor judgment. Force-push, tags, and publish stay forbidden regardless. Grant the push and I dispatch the workflow, compare the windows-2022 QPC winner to the frozen windows-2025 result, and record the row-4 verdict; a flip here would be unexpected (§5.2 says escalate).

- 2026-07-15 🟠 OPEN — filed; agent moved on to other work.
- 2026-07-15 — DE-RISKED; two verified findings, both confirming the push is genuinely needed (no executor shortcut). (1) The row-4 check is MEANINGFUL, not moot: Windows `Instant` runs a real 3-way runtime tournament (QPC / InterruptTimePrecise / UnbiasedInterruptTimePrecise — `src/arch/fallback.rs`, `SOURCE_COUNT=3`, `INSTANT_QPC` is only the probe default), so a windows-2022 ↔ windows-2025 selection flip IS possible and worth checking. (2) The environment IS provisionable: `windows-2022` remains an available, supported GitHub-hosted runner as of 2026-07 (GitHub N-1 image policy, maintained ~through 2028; the June-2026 migration only moves what `windows-latest`/`windows-2025` point to for Visual Studio, NOT windows-2022 availability), so row 4 cannot be dispositioned as a "no second Windows environment" class-1 residual the way row 6 (mac-x86, discontinued Intel Macs) was. Concrete edit the push enables: `.github/workflows/bench-speed-windows.yml` hardcodes `windows-2025` in the x86_64 job (`runs-on` line 27, tag `github-windows-2025-x86_64`, output/artifact paths lines 51/61/62); the row-4 run adds a mirror job or a matrix `runner` entry with `windows-2022` labels + a `github-windows-2022-x86_64` tag, extracts its `selected_provider`, and compares to the frozen windows-2025 bundle. Did NOT build the speculative CI edit — job-vs-matrix structure is an owner call and it produces nothing without the push. Grant the push → I make the edit, dispatch, compare the windows-2022 winner to windows-2025, and record the row-4 verdict.
- 2026-07-16 ☑️ RESOLVED — the blocker genuinely disappeared.

## `ESC-THREAD-PMU-X86-PROBE`

- **Status:** ☑️ RESOLVED
- **Filed:** 2026-07-15 by unattended agent
- **Owner decision needed:** Freeze row 2 (T-LINUX-X86, c5n.metal thread-CPU) needs a NEW x86 thread-pmu probe: benches/probes/aarch64-thread-pmu.c is 361 lines of arch-specific perf-mmap seqlock + rdpmc(pmccntr_el0) C; the rdpmc-via-mmap path is only exercisable on cap_user_rdpmc=true bare metal (c5n.metal ~$3.89/hr), untestable on macOS. Verified no shortcut: the speed bundle records only instant/ordered selection, not thread_cpu, so run-speed-aws.sh on metal can't answer it. OPTIONS: (a) authorize a focused pass to adapt the probe to x86 rdpmc, validate compile/run on a cheap c7i VM, then run on c5n.metal (perf-mmap wins -> tournament stays with evidence; syscall wins -> convert to capability policy); (b) ACCEPT the retained runtime tournament as row 2's terminal verdict — it is the conservative, correct-either-way default (handles perf-mmap and syscall at runtime), and the metal probe would only enable an M2 SIMPLIFICATION, not fix any correctness gap. RECOMMEND (b) unless the simplification is wanted. The frozen c7i evidence already shows capability != profitability (perf available-but-slower there).

- 2026-07-15 🟠 OPEN — filed; agent moved on to other work.
- 2026-07-15 — DE-RISKED via code investigation. tach's Linux thread-CPU inline path (`src/arch/thread_cpu_linux_inline.rs`) uses `PERF_COUNT_SW_TASK_CLOCK` + `CAP_USER_TIME` (the task-clock software event read through the mmap page's `time_mult`/`time_shift` + a TSC read) — it does NOT use the `cap_user_rdpmc` hardware-cycles path. So the plan's "cap_user_rdpmc metal" premise is imprecise for THIS provider, and the correct x86 probe is far simpler than the aarch64 template: reuse `read_task_clock` verbatim (its seqlock + cap_user_time conversion are arch-independent) and swap `read_cntvct()` (mrs cntvct_el0) → an x86 `rdtsc`, compared against `native_thread_ns()` (syscall CLOCK_THREAD_CPUTIME_ID) with the busy-interval self-check (task-clock Δ must ≈ syscall Δ). DROP the entire `read_pmccntr`/`read_pmccntr_ns` block — it is the Graviton3-hardcoded (13/5 cycles/ns) `index==32` rdpmc diagnostic, not tach's path. Two even-lower-risk options: reuse tach's own `BenchPerfCounterEvidence`/`PerfCounterSelectionEvidence` (candidate_names[5], candidate_batches_ns, selected_candidate) if a bench surfaces it; or note that c5n.metal may not be required at all since cap_user_time (not cap_user_rdpmc) is the mechanism and Nitro c7i already exposes an inline TSC — the frozen c7i "perf available-but-slower" may already be the answer (syscall wins → capability policy). Recommendation stands: accepting the tournament (option b) is safe; if the simplification is wanted, the build is now a bounded ~40-line task validated on a cheap c7i, not the feared 361-line untestable rewrite.
- 2026-07-15 ☑️ RESOLVED — executed option (a) under the owner's standing "drive the §5.2 probe table" instruction (row 2 is a §5.2 probe). Built `benches/probes/x86-thread-pmu.c` and arch-generalized `benches/run-thread-pmu-aws.sh` (`7e99554`), validated on a cheap `c7i.large` Nitro VM (~$0.02: `cap_user_time` absent, `caps=0x2` → syscall by necessity), then ran the plan's enumerated `c5n.metal` (~$0.65, self-terminated, no orphan: `cap_user_time` present `caps=0x1a`, perf task-clock mmap 22.25 ns vs raw syscall 728.71 ns — ~33× faster and self-check-correct over a 50 ms busy interval). VERDICT: `T-LINUX-X86` thread-CPU is an **availability-preferred capability gate** (perf-mmap when `cap_user_time` is exposed — bare metal; raw `CLOCK_THREAD_CPUTIME_ID` syscall when absent — Nitro VMs), NOT a measured tournament (no speed flip: perf wins by 33× whenever available). This also corrected the matrix's imprecise "capability does not determine profitability" note (the Nitro capability is *absent*, not present-but-unprofitable). Evidence `EVID-THREAD-CPU-X86`. The metal spend was the plan's enumerated environment and is reversible (local commit). M1.G1 does not close on this alone — row 4 (`ESC-WINDOWS-2022-PUSH`) remains owner-blocked.

## `ESC-M1-FREEZE-CLOSURE`

- **Status:** ☑️ RESOLVED
- **Filed:** 2026-07-15 by unattended agent
- **Blocks gate:** `OBJ-SIMPLIFY-TIMERS.M1.G1`
- **Owner decision needed:** M1.G1 (freeze table) is 6/7 rows verdicted with committed evidence (rows 1/3/5 no-flip fixed, row 2 capability gate EVID-THREAD-CPU-X86, rows 6/7 class-1 residual). The sole remaining row W/O-WINDOWS (row 4) requires the owner-authorized windows-2022 push tracked in ESC-WINDOWS-2022-PUSH; plan §5.2 line 190 prescribes dispatching the Windows bench workflow on the windows-2022 runner and §1.1/line 61 flags that push as owner-authorization-required. No executor path exists: an AWS-windows substitute would be an unenumerated plan deviation. Verified the row is meaningful (Windows Instant is a real 3-way tournament) and the runner is available (windows-2022 supported through ~2028). Parking M1.G1 on the owner until the push is granted (then I edit/dispatch/compare/record) or row 4 is dispositioned.

- 2026-07-15 🟠 OPEN — filed; agent moved on to other work.
- 2026-07-16 ☑️ RESOLVED — the blocker genuinely disappeared.

## `ESC-APPLE-ORDERED-SELECTION`

- **Status:** ✅ ACCEPTED
- **Filed:** 2026-07-16 by unattended agent
- **Blocks gate:** `OBJ-SIMPLIFY-TIMERS.M2.G1`
- **Owner decision needed:** Apple OrderedInstant (O-MAC-A64) runtime selection contradicts its frozen description, is non-deterministic across process starts, and selects an unbarriered provider — three entangled concerns on the flagship happens-before contract, surfaced while preparing the M2 fixed-pick conversion. OBSERVED on catalyst (M1 Max; user_timebase_mode=3 NOSPEC_APPLE, cont_hwclock=1, cntfrq=24MHz), 5 clean process starts: Instant selects apple_bare_cntvct every run (stable, matches the frozen W-MAC-A64 verdict); the ORDERED selector selects apple_mach_absolute_time (runs 1,2,4) or apple_mach_continuous_time (runs 3,5) — NEVER the 'barriered isb+CNTVCT self-synchronizing direct route' the provider-policy-matrix O-MAC-A64 row claims. The direct acntvct offset/base routes reliably LOSE (mach_absolute at candidate index 2 must beat the index-0 incumbent materially to win, so this is structural, not noise). Concern 1 (claim unsupported): EVID-APPLE-BARE-CNTVCT records only primitive read-costs, never a live ordered-selector observation, so the matrix 'self-sync route selected' claim is inference and is contradicted by behavior (same class as the already-corrected mac-x86 'selected invariant TSC' error). Concern 2 (sleep-domain non-determinism): mach_absolute excludes sleep, mach_continuous includes it, so the cross-start coin-flip makes Apple ordered suspend semantics non-deterministic — entangled with the OPEN owner-gated 5.1(d) suspend measurement. Concern 3 (missing barrier): fallback::mach_time() is a bare mach_absolute_time() FFI call with no isb/dmb/dsb, so the selected ordered provider appears to lack the synchronization edge OrderedInstant's contract requires, yet ordered_honors_happens_before is certified at 0 violations — an unexplained inconsistency. IMPACT: M2 cannot convert O-MAC-A64 to a deterministic fixed pick without an owner ruling on the intended ordered provider and sleep domain; the ordered tournament stays in place (retained-as-measured) until then. W-MAC-A64 Instant is UNAFFECTED and its conversion proceeds. RECOMMEND: deterministic apple_mach_absolute_time if it is shown to carry the ordering edge (agrees with bare on M1 at delta 0, matches the XNU-designated absolute/exclude-sleep domain); otherwise the barriered isb+CNTVCT direct route (~2x slower but contract-correct).

- 2026-07-16 🟠 OPEN — filed; agent moved on to other work.
- 2026-07-16 ✅ OWNER RULING — **Option A: self-synchronizing counter, mode-gated.** `OrderedInstant` fixed pick = `ACNTVCT_EL0` (mode 3) / `CNTVCTSS_EL0` (mode 2) / `isb sy; cntvct` fallback (else), all offset-corrected into the Mach absolute domain. Rationale accepted: the self-sync register carries the same architectural ordering guarantee as the explicit `isb` barrier at lower cost (5.06/3.55 ns vs 10.22/8.66 ns M1/M4), confirmed 0 violations / ~0.88e9 reads on both machines (`EVID-APPLE-ORDERED`). `Instant` stays bare `CNTVCT_EL0`. Executor now converts `apple_aarch64.rs` as one commit (Instant=bare unchanged + Ordered=this mode gate, mirroring the x86 LFENCE capability-gate pattern); `M2.G1` unblocks on landing + CI `native/macos` green. Blocker (owner decision) removed.
- 2026-07-16 — DE-RISKED via a cross-thread happens-before survey on both machines (`EVID-APPLE-ORDERED`, SHA `1e7ec88`; probe `3f9050e`+`1e7ec88`). Direct measurement of each candidate under a migrating-thread stress loop (negative control `bare_cntvct`, positive control `isb+cntvct`): bare fires 112.7M violations / 942.9M reads (10.8% M1 Max, 12.5% M4 Pro), while `mach_absolute`, `acntvct` (self-sync), and `isb+cntvct` each hold **0 / ~0.87e9 per machine**. **Concern 3 resolved:** the selected provider is ordered — the ordering the contract needs (timer read not hoisted above a prior `Acquire` load) is supplied by the opaque FFI call boundary for `mach`, and architecturally by the self-sync registers; measured clean at the same scale bar `Instant` bare CNTVCT cleared. **Concern 2 dissolves under any fixed pick:** all three ordered candidates read the Mach absolute (exclude-sleep) domain, so a deterministic pick ends the cross-start coin-flip. **Concern 1 (claim vs behavior):** a fixed pick of the self-sync route makes the code finally match the matrix's original "self-synchronizing direct route" intent. Frozen `EVID-APPLE-BARE-CNTVCT` speed line: `acntvct` 5.06/3.55 ns < `mach` 5.38/3.78 ns < `isb+cntvct` 10.22/8.66 ns (M1/M4). **STILL OPEN — owner philosophy pick:** (A) fastest measurement-eligible = `acntvct` self-sync, gated to `isb+cntvct` where the CPU mode exposes no self-sync register (the x86-LFENCE-gate pattern); (B) conservative = always `isb+cntvct` (~2× slower, no mode gate); (C) match quanta = `mach_absolute_time()` (call-boundary ordering, opaque libSystem). All three are ordered-eligible; RECOMMEND (A) — same architectural guarantee as (B) at lower cost, confirmed by measurement. On the ruling, Apple converts as one commit (Instant=bare stable + Ordered=chosen pick) and M2.G1 unblocks.
- 2026-07-16 ✅ ACCEPTED — owner accepts the limitation; land the ADR and exit via the gate's Fallback.

## `ESC-FREEBSD-PARITY-MEASUREMENT`

- **Status:** 🟠 OPEN
- **Filed:** 2026-07-16 by unattended agent
- **Blocks gate:** `OBJ-SIMPLIFY-TIMERS.M2.G1`
- **Owner decision needed:** FreeBSD-x86 inline parity (M2.G1 condition 3) is unmeasured: no GitHub-hosted FreeBSD runner exists. The 4 other non-Apple families (linux-x86_64/aarch64, windows-x86_64/aarch64) are now CI-verified within max(1ns,5%) (run 29553989072, 16/16 leaves pass); Apple verified locally. OPTIONS: (a) run the FreeBSD parity bench on AWS now (SPEND, run-speed-freebsd-aws.sh, self-terminating ~$0.10) -> measured in M2; (b) DISPOSITION: defer to M3's FreeBSD-AWS evidence regen (which runs FreeBSD on AWS anyway) + cover-by-analogy now (FreeBSD Instant=RDTSC / Ordered=LFENCE+RDTSC are the IDENTICAL picks the CI-verified linux-x86 uses). RECOMMEND (b) -- no premature spend; the harness already emits freebsd-wall-selection.json and check-inline-parity.py covers it, so it is a runner question not a tooling gap.

- 2026-07-16 🟠 OPEN — filed; agent moved on to other work.

## `ESC-M3-CLAIMS-REMEASURE`

- **Status:** 🟠 OPEN
- **Filed:** 2026-07-16 by unattended agent
- **Blocks gate:** `OBJ-SIMPLIFY-TIMERS.M3.G1`
- **Owner decision needed:** M3.G1 §7.3 (truthful claims) needs (a) fresh six-cell SPEED numbers re-measured on the converted tree — tach vs quanta/fastant/minstant/std — which requires AWS bench runs for the cross-arch cells (c7i Intel, c7g Graviton, FreeBSD via run-speed-{aws,freebsd-aws}.sh) [SPEND] + the Windows dispatch; the Apple-aarch64 flagship cell + macos are locally measurable. (b) Rewriting tach's PUBLIC speed claims (README/BENCHMARKS) to those numbers + ADR-0005 eligibility framing — the owner's domain, and publishing stays the owner's act. Also folds in: updating the stale speed_evidence.py validate_*_wall_selector validators from runtime_tournament -> fixed-pick shape, and reconciling residual internal-doc refs to the §7.1-deleted apparatus (ORDERED-VERIFICATION.md, evidence-dir READMEs, route-coverage.toml) + the deferred .tgz bundles. All executor-doable pieces are COUPLED to the spend-gated fresh evidence, so they land together in §7.3. OPTIONS: (a) authorize the AWS six-cell re-measurement + collaborate on claims wording; (b) rescope §7.3. RECOMMEND (a) when ready. This + ESC-FREEBSD-PARITY are the only gates between here and the approval packet; everything else is done and CI is fully green.

- 2026-07-16 🟠 OPEN — filed; agent moved on to other work.
